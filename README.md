# MMSE

Place used to build the graph intelligence (without vibe coding)



# How to run

Make sure you added your `vars.local.env`

`uv install`

`uv venv`

`uv sync`

Start agent
`uv run streamlit main.py`

Start UI
`uv run python small-flask-app/app.py`